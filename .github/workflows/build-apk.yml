name: Build Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: npm install

      - name: Prepare Web Assets for Android
        run: |
          node -e "
            const fs = require('fs');
            const path = require('path');
            const target = 'android/app/src/main/assets/public';
            if (!fs.existsSync(target)) fs.mkdirSync(target, { recursive: true });
            fs.copyFileSync('tracker.html', path.join(target, 'index.html'));
            fs.copyFileSync('manifest.json', path.join(target, 'manifest.json'));
            console.log('✅ Web assets copied directly to Android assets/public');
          "

      - name: Build Android APK with Gradle
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: TOP-GPS-Tracker-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
